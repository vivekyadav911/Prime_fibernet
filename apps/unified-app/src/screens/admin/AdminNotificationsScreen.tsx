import { EmptyState, Screen } from '@prime/ui';

export function AdminNotificationsScreen() {
  return (
    <Screen>
      <EmptyState title="Bulk notifications" description="Audience selector + delivery report (ADM-007)" />
    </Screen>
  );
}
