import { EmptyState, Screen } from '@prime/ui';

export function AdminSettingsScreen() {
  return (
    <Screen>
      <EmptyState title="Settings" description="Company branding, feature flags (ADM-009)" />
    </Screen>
  );
}
