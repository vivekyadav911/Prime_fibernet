import { EmptyState, Screen } from '@prime/ui';

export function AdminRequestsScreen() {
  return (
    <Screen>
      <EmptyState title="Request oversight" description="Assign, prioritize, escalate (ADM-005)" />
    </Screen>
  );
}
