import { EmptyState, Screen } from '@prime/ui';

export function AdminOfficersScreen() {
  return (
    <Screen>
      <EmptyState title="Officer management" description="Invite, assign regions, shifts (ADM-003)" />
    </Screen>
  );
}
