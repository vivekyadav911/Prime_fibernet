import { EmptyState, Screen } from '@prime/ui';

export function AdminAnalyticsScreen() {
  return (
    <Screen>
      <EmptyState title="Analytics" description="Revenue, TAT, attendance reports (ADM-006)" />
    </Screen>
  );
}
