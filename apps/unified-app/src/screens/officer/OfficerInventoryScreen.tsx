import { EmptyState, Screen } from '@prime/ui';

export function OfficerInventoryScreen() {
  return (
    <Screen>
      <EmptyState title="Inventory" description="Assigned equipment list (OFF-005)" />
    </Screen>
  );
}
